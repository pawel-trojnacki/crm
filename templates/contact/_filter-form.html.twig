<form method="GET" class="d-flex align-items-end">
    <div class="pe-2">
        <input type="search" name="search" id="search" class="form-control" value="{{search}}" placeholder="Search">
    </div>
    <div class="pe-2">
        <select name="order" id="order" class="form-select">
            {% if not order %}
            <option value="" disabled selected hidden>Sort by</option>
            {% endif %}
            {% for val, name in sortOptions %}
            <option value="{{val}}" {{ (val==order) ? 'selected' : '' }}>{{name}}</option>
            {% endfor %}
        </select>
    </div>
    <div class="pe-2">
        <button type="submit" class="btn btn-primary">Apply</button>
    </div>
    <div>
        <a href="{{ path('app_contact_index', {slug: workspace.slug}) }}" class="btn btn-secondary">Clear</a>
    </div>
</form>