<aside class="sidebar">

  <a class="brand fs-4 fw-bold lh-1 mb-5" href="#">CRM</a>

  <nav>
    <h2 class="fs-6 fw-normal text-muted text-uppercase">Workspace</h2>
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href="{{ path('app_dashboard_index', { slug: app.user.workspace.slug }) }}">
          <i class="bi bi-grid"></i>
          <span class="ps-1">
            Dashboard
          </span>
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('app_team_index', { slug: app.user.workspace.slug }) }}">
          <i class="bi bi-people"></i>
          <span class="ps-1">
            Team
          </span>
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('app_contact_index', { slug: app.user.workspace.slug }) }}">
          <i class="bi bi-briefcase"></i>
          <span class="ps-1">
            Contacts
          </span>
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('app_company_index', { slug: app.user.workspace.slug }) }}">
          <i class="bi bi-folder"></i>
          <span class="ps-1">
            Companies
          </span>
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('app_deal_index', { slug: app.user.workspace.slug }) }}">
          <i class="bi bi-check2-square"></i>
          <span class="ps-1">
            Deals
          </span>
        </a>
      </li>
    </ul>
  </nav>

  <nav>
    <h2 class="fs-6 fw-normal text-muted text-uppercase">Your account</h2>
    <ul class="navbar-nav">
      <li>
        <a class="nav-link" href="#">
          <i class="bi bi-person"></i>
          <span class="ps-1">
            Your Profile
          </span>
        </a>
      </li>
      <li>
        <a class="nav-link" href="/logout">
          <i class="bi bi-box-arrow-left"></i>
          <span class="ps-1">
            Logout
          </span>
        </a>
      </li>
    </ul>
  </nav>

</aside>