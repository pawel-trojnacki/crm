{% extends 'base.html.twig' %}

{% block title %}
	Meetings
{% endblock %}

{% block heading %}
	Meetings
{% endblock %}

{% block body %}
<div class="d-flex justify-content-between mb-4">

	<div class="card">
		<div class="card-body d-flex">
			{{ include('partials/_importance-item.html.twig', {
				item_text: 'Low importance',
				item_color: '#0fcce3', 
			}) }}
			{{ include('partials/_importance-item.html.twig', {
				item_text: 'Normal importance',
				item_color: '#ef6e4b', 
			}) }}{{ include('partials/_importance-item.html.twig', {
				item_text: 'High importance',
				item_color: '#f82362', 
			}) }}
		</div>
	</div>

	<div class="d-flex flex-column justify-content-center">
		<a href="{{ path('app_meeting_create', { slug: app.user.workspace.slug }) }}"class="btn btn-primary">
			Add meeting
		</a>
	</div>
	
</div>

	<div id="calendar-holder" data-events-url="{{ path('fc_load_events') }}"></div>
{% endblock %}
